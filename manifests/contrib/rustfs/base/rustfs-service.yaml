apiVersion: v1
kind: Service
metadata:
  name: rustfs
  labels:
    app: rustfs
    component: objectstorage
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 9000
    targetPort: 9000
    protocol: TCP
  - name: console
    port: 9001
    targetPort: 9001
    protocol: TCP
  selector:
    app: rustfs
