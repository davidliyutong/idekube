apiVersion: v1
kind: Service
metadata:
  name: clash
  labels:
    app: clash
spec:
  type: ClusterIP
  ports:
  - name: http-proxy
    port: 7890
    targetPort: 7890
    protocol: TCP
  - name: dashboard
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: clash
